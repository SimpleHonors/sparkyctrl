# Real host address book (contains LAN addresses) — never commit
hosts.toml
.sparkyctrl/

# Build artifacts
/sparkyctrl
/dist/
*.exe
*.test
*.out

# Env / secrets
.env
*.local

# Real client tokens — never commit
tokens
.sparkyctrl/tokens

# Internal dev-process notes (brainstorming specs, plans, verification) — keep out of the repo
docs/superpowers/

# OS / editor cruft
.DS_Store
